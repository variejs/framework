import { injectable } from "inversify";
import Validator from "varie/lib/validation/Validator";

@injectable()
export default class temp extends Validator {
  public rules = {
    //
  };

  public messages = {
    //
  };
}
